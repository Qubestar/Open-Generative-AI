# Directives

SOPs written in Markdown. Each file describes:
- **Goal** — what this directive accomplishes
- **Inputs** — what data / config it needs
- **Tools/Scripts** — which `execution/` script to call
- **Outputs** — what it produces
- **Edge Cases** — known gotchas

Directives are living documents. Update them as you learn.
